[project]
name = "tiny-transformer-lab"
version = "0.0.0"
requires-python = ">=3.10"
dependencies = [
  "typer>=0.12",
  "pyyaml>=6",
  "tokenizers>=0.14,<0.16",
]

[project.optional-dependencies]
mlflow = ["mlflow>=2.10"]

[tool.black]
line-length = 100

[tool.ruff]
line-length = 100
